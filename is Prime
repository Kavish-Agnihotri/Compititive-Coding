// Solved on Interview bit 
int Solution::isPrime(int A) {
    if(A==1)
    return 0;
    for(int i=2;i*i<=A;i++)
    {
        // Cofactor is a divides b then n/a also divides b wiseversa also true so no need to check
        if(A%i==0)
            return 0;
    }
    return 1;
}
